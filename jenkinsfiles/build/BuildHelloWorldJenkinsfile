node {
    timeout(15) { // in minutes
        def cfg = [
                string        : env.STRING.trim(),
        ]
        doWork(cfg)
    }
}
 
def doWork(cfg) {
    stage("Prepare") {
        echo "Hello world $cfg.string"
    }
  }